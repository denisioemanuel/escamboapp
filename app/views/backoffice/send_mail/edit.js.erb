$(document).ready(function(){
	let $sendModal = $("#sendMailModal");

	$("#exampleModalLabel").text("<%= I18n.t('labels.send_message_to', name: @admin.name) %>");
	$("#recipient-text").val("<%= @admin.email %>");

	$sendModal.modal();

	$sendModal.on('shown.bs.modal', function () {
  		$('#subject-text').focus();
	});

});